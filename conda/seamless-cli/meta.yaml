{% set version = "0.14" %}
package:
    name: seamless-cli
    version: {{ version }}

build:
  number: 4
  noarch: python

requirements:
  build:
    - python
  run:
    - openssl
    - psutil
    - peewee
    - sqlite
    - aiohttp
about:
  summary: This is a collection of bash scripts wrapping the Seamless Docker image.
